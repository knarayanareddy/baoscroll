import './style.css';
import { ClockmakerStoryExperience } from './production/ClockmakerStoryExperience.js';
const titles=['The Workshop Wakes','The City Disagrees','The Impossible Gear Tower','The Remembered Hour','The Final Hour','A New Clock Ticks'];
const beats=['The production foundation is waking.','The city model waits for its rebuilt scene.','Every tooth locks behind the apprentice.','The workshop remembers every hand that made it.','The master clock waits for its rebuilt choice.','A new city waits for its rebuilt dawn.'];
const exp=new ClockmakerStoryExperience(document.getElementById('clockmaker-canvas'));window.__clockmakerProduction=exp;const progress=document.getElementById('progress'),title=document.getElementById('title'),beat=document.getElementById('beat'),chapter=document.getElementById('chapter');let last=performance.now();function frame(now){const max=Math.max(1,document.documentElement.scrollHeight-innerHeight),p=Math.max(0,Math.min(1,scrollY/max)),dt=Math.min(.05,(now-last)/1000);last=now;exp.tick(p,dt);title.textContent=titles[exp.activeChapter];beat.textContent=beats[exp.activeChapter];chapter.textContent=`CHAPTER ${exp.activeChapter+1} — ${exp.activeChapter===3?'PRODUCTION BENCHMARK ACTIVE':'SCENE REBUILD QUEUED'}`;progress.style.transform=`scaleX(${p})`;requestAnimationFrame(frame);}requestAnimationFrame(frame);
